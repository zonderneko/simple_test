package interfaces;

import testCase.Autorization;
import testCase.DropdownMenuURLs;

public interface IBase {
    Autorization auth = new Autorization();
    DropdownMenuURLs ddown = new DropdownMenuURLs();
}
